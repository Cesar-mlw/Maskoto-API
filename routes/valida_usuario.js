let db = require("../db.js")

module.exports = app => {
    app.route("/validate_user")
        .post((req, res) => {
                //app.db.connection.fn('u179156626_anima.valida_usuario', req.body.email, req.body.senha)
            })
        }